<% if @picture.new_record? %>
  alert("<%= j t('messages.piture.error', error: @picture.errors.full_messages.join(', ').html_safe) %>");
<% else %>
  $("#pictures").append("<%= j render(@picture) %>");
<% end %>